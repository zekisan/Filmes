@model Filmes.Models.Filme

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Filme</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Titulo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Titulo)
            @Html.ValidationMessageFor(model => model.Titulo)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TituloOriginal)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TituloOriginal)
            @Html.ValidationMessageFor(model => model.TituloOriginal)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DataLancamento)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DataLancamento)
            @Html.ValidationMessageFor(model => model.DataLancamento)
        </div>

        <div>
            @Html.Action("_GenerosForm","Filme")
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
